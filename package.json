{
  "name": "battleframe-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && npm run build --workspaces --if-present",
    "test": "vitest run",
    "docs": "typedoc",
    "docs:module": "typedoc --options typedoc.full-thrust.json"
  },
  "devDependencies": {
    "typedoc": "^0.26.11",
    "typescript": "^5.5.4",
    "vite": "^5.4.0",
    "vitest": "^2.0.5"
  }
}
